<section class="details-hero">
    <div class="container">
        <h2>Laptop Details</h2>
    </div>
</section>

<section class="details-content">
    <div class="container">
{{#with device}}

        <div class="product-details">
            <div class="product-image">
                <img src="{{image}}" alt="Laptop">
            </div>
            <div class="product-info">
                <h3>Brand: {{brand}}</h3>
                <p><strong>Model:</strong> {{model}}</p>
                <p><strong>Hard Disk:</strong> {{hardDisk}}</p>
                <p><strong>Screen Size:</strong> {{screenSize}}</p>
                <p><strong>RAM:</strong> {{ram}}</p>
                <p><strong>Operating System:</strong> {{operatingSystem}}</p>
                <p><strong>CPU:</strong> {{cpu}}</p>
                <p><strong>GPU:</strong> {{gpu}}</p>
                <p><strong>Price:</strong> ${{price}}</p>
                <p><strong>Color:</strong> {{color}}</p>
                <p><strong>Weight:</strong> {{weight}}</p>
            </div>
        </div>
{{/with}}

        {{#if user}}
        <div class="product-actions">
            {{#if isOwner}}
            <button class="btn edit"><a href="/devices/{{id}}/edit">Edit</a></button>
            <button class="btn delete"><a href="/devices/{{id}}/delete">Delete</a></button>
            {{else}}
                {{#if hasPreferred}}
                <p class="prefer-message">You've already preferred this device</p>
                {{else}}
                <button class="btn prefer"><a href="/devices/{{id}}/prefer">Prefer</a></button>
                {{/if}}
            {{/if}}
        </div>
        {{/if}}

    </div>
</section>