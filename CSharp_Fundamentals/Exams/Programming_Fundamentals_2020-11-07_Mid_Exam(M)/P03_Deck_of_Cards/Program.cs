using System;
using System.Collections.Generic;
using System.Linq;

namespace _03_Deck_of_Cards
{
    class Program
    {
        static void Main(string[] args)
        {

            List<string> tomsCars = Console.ReadLine()
                                           .Split(',', StringSplitOptions.RemoveEmptyEntries)
                                           .ToList();
            int nComands = int.Parse(Console.ReadLine());

            List<string> comand = Console.ReadLine().Split().ToList;

            for (int i = 0; i < nComands - 1; i++)
            {



            }
        }
    }
}
